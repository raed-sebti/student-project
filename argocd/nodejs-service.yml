apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  namespace: student-project 
spec:
  type: NodePort
  selector:
    app: nodejs
  ports:
    - protocol:   TCP
      port:       4000
      targetPort: 4000
      nodePort:   31080
